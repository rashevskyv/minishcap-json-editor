from PyQt5.QtGui import QColor

PROBLEM_DEFINITIONS = {
    # Наразі порожньо, можна додати правила валідації пізніше
}

DEFAULT_DETECTION_SETTINGS = {
    # Наразі порожньо
}

DEFAULT_AUTOFIX_SETTINGS = {
    # Наразі порожньо
}